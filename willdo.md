### 界面体验

- **表格总览模式**：在折叠框上方加一个紧凑表格，一行一个测试点，列名：编号、名称、结果、时间、空间。折叠框作为详情展开，表格作为总览
- **拖拽文件**：支持直接拖拽 .cpp 文件和 .in/.out 文件到窗口，替代手动点选

### 测评功能

- **自定义比较器（Special Judge）**：支持用户指定一个 checker 程序，用于非精确匹配的评测（如浮点误差、多解问题）
- **一键重测**：上次评测参数不变，只重新编译和运行，省去重新选择的步骤
- **随机测试生成器**：支持配置一个数据生成器，一键生成随机测试数据并自动评测

### 输出与调试

- **差异比较高亮到字符级**：目前差异是行级，可以进一步高亮行内具体不同的字符
- **折叠框内显示返回码**：RE 时在折叠框内显示具体退出码，方便调试
- **超时/超内存时显示实际值**：TLE/MLE 时在折叠框内显示实际耗时/实际内存，方便判断差多少

### 稳定性与兼容性

- **中断评测**：评测进行中支持点击按钮中断，目前只能等全部跑完