## 功能增强

- [x] **多线程并行评测**：目前是串行逐个测试，可以加一个"并行数"选项，多个测试点同时运行，大幅缩短总评测时间
- [ ] **Special Judge**：支持自定义评测脚本（spj），当输出不是简单文本比较时（如浮点数误差、多种合法输出），用户可以指定一个 checker 程序
- [ ] **子任务/捆绑测试**：支持把若干测试点编为一组，组内全 AC 才算该组通过，常见于 OI 赛题评分
- [ ] **评测历史记录**：保存最近 N 次评测结果，可以回看对比，看改了代码后是变快了还是变慢了
- [ ] **拖拽添加文件**：支持直接把 .cpp 或 .in/.out 文件拖进窗口，而不必每次点按钮浏览

## 评测精度

- [x] **高精度计时**：用 `QueryPerformanceCounter` 替代 `time.perf_counter`，精度从微秒级提升到纳秒级
- [x] **进程级硬限制**：目前 Job Object 只做了 `KILL_ON_JOB_CLOSE`，可以加上 `JOB_OBJECT_LIMIT_PROCESS_TIME` 和 `JOB_OBJECT_LIMIT_PROCESS_MEMORY`，让操作系统在超限瞬间就杀进程，而不是靠轮询采样（20ms 间隔可能有误差）
- [x] **返回码细分**：RE 时区分信号类型（如 SIGSEGV / SIGABRT / SIGFPE），给出更有信息量的报错

## UI/UX

- [ ] **进度条**：加一个进度条显示当前评测进度（如 3/10），比纯文字更直观
- [ ] **全部展开/全部折叠按钮**：一键展开或折叠所有测试点结果
- [ ] **按结果筛选**：加一排按钮 `AC | WA | TLE | MLE | RE | All`，点击只显示对应结果的折叠框
- [ ] **表格总览模式**：在折叠框上方加一个紧凑表格，一行一个测试点，列名：编号、名称、结果、时间、空间。折叠框作为详情展开，表格作为总览
- [x] **窗口记忆**：记住窗口位置、大小、上次选择的文件路径、限制参数等，下次打开自动恢复（写一个小的 config.json）

## 稳定性

- [x] **输出大小限制**：如果程序输出极大（如死循环疯狂输出），目前会把所有 stdout 吃进内存。可以加一个输出上限（如 64MB），超了直接判 RE
- [x] **临时文件清理**：如果程序异常退出或用户中途关闭窗口，确保临时编译的 exe 和工作目录被清理