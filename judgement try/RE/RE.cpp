#include <bits/stdc++.h>
#define ll long long
using namespace std;

int main() {
    cin.tie(nullptr)->sync_with_stdio(false);
    int n;
    cin >> n;
    if(n == 10){
        int p = 1 / 0;
        cout << p << endl;
    }
    if(n == 11){
        int p = 1 % 0;
        cout << p << endl;
    }
    if(n == 20){
        int a[1] = {1};
        cout << a[1] << endl;
    }
    cout << 1 << endl;
}